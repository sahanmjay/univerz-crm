{
  "name": "univerz-crm",
  "version": "1.0.0",
  "private": true,
  "description": "Univerz CRM — internal call & appointment management for a 6-person creative agency",
  "scripts": {
    "dev": "vite",
    "build:hr": "npm --prefix team-task-manager install && npm --prefix team-task-manager run build",
    "build": "npm run build:hr && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
